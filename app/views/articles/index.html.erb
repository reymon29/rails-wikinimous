<h1>Welcome</h1>
<% @articles.each do |article| %>
  <h4><%= link_to article.title, article_path(article) %></h4>
  <p><%= truncate article.content, length: 10 %></p>

<% end %>
<%= link_to "New", new_article_path %>
